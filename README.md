# Cloudflare worker to bridge tradingview.com webhooks to notfy.sh phone notifications.

Use `worker-build` for building. (Builds to wasm to be a cloudflare worker).
use `cargo test` for a native test
