name = "phone-web-hook"
main = "build/worker/shim.mjs"
compatibility_date = "2026-03-12"

[build]
command = "cargo install -q worker-build && worker-build --release"

[vars]
NTFY_TOPIC = "matiu.dev/tradingview-ntfy-bridge"
